# Optional: only needed for gated Hugging Face datasets.
HF_TOKEN=hf_your_token_here

# MongoDB
MONGO_URI=mongodb://localhost:27017
MONGO_DB=student_feedback_db
FEEDBACK_COLLECTION=feedbacks
MONGO_TIMEOUT_MS=2000

# Project directories (relative paths are resolved from the repository root)
MODEL_DIR=models
DATA_DIR=data
